// Initialization for ES Users
// import { Ripple, initMDB } from "mdb-ui-kit";

// initMDB({ Ripple });

const mms=document.querySelector("#MMS")

mms.addEventListener("select",()=>{
    console.log('sarvar');
})